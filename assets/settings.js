const TRANSITION_TIME_MS = 250;       // => $transition-time-ms
const DPI_DEFAULT = 80;
const ZOOM_RATIO = 120 / DPI_DEFAULT; // => $zoom-ratio
const FRAME_RULER_WIDTH_NOMINAL = 32; // => $frame-ruler-width-nominal

export default {
  TRANSITION_TIME_MS,
  DPI_DEFAULT,
  ZOOM_RATIO,
  FRAME_RULER_WIDTH_NOMINAL,
}
